package com.dc.commonlib.utils.video;

import android.text.TextUtils;



import java.util.ArrayList;

public class ParseUtlis {

/*    public static  ArrayList<ContractAccessoryEntity> makeGalleryList(SafeInspectEntity.ResultsBean.AttachmentsBean attachmentBean, Boolean isEdit) {
        if (attachmentBean == null) {
            return null;
        }
        ArrayList<ContractAccessoryEntity> listData = new ArrayList<>();
        if (!TextUtils.isEmpty(attachmentBean.getAttachment1())) {
            ContractAccessoryEntity accessoryEntity = new ContractAccessoryEntity();
            accessoryEntity.setAddView(isEdit);
            accessoryEntity.setFileUrl(attachmentBean.getAttachment1());
            accessoryEntity.setId(attachmentBean.getAttachment1());
            accessoryEntity.setNetPic(true);
            accessoryEntity.setPosition(1);
            accessoryEntity.setAttachment1(attachmentBean.getAttachment1());
            listData.add(accessoryEntity);
        }
        if (!TextUtils.isEmpty(attachmentBean.getAttachment2())) {
            ContractAccessoryEntity accessoryEntity = new ContractAccessoryEntity();
            accessoryEntity.setAddView(isEdit);
            accessoryEntity.setFileUrl(attachmentBean.getAttachment2());
            accessoryEntity.setId(attachmentBean.getAttachment2());
            accessoryEntity.setNetPic(true);
            accessoryEntity.setPosition(2);
            accessoryEntity.setAttachment2(attachmentBean.getAttachment2());
            listData.add(accessoryEntity);
        }
        if (!TextUtils.isEmpty(attachmentBean.getAttachment3())) {
            ContractAccessoryEntity accessoryEntity = new ContractAccessoryEntity();
            accessoryEntity.setAddView(isEdit);
            accessoryEntity.setFileUrl(attachmentBean.getAttachment3());
            accessoryEntity.setId(attachmentBean.getAttachment3());
            accessoryEntity.setAttachment3(attachmentBean.getAttachment3());
            accessoryEntity.setPosition(3);
            accessoryEntity.setNetPic(true);
            listData.add(accessoryEntity);
        }
        if (!TextUtils.isEmpty(attachmentBean.getAttachment4())) {
            ContractAccessoryEntity accessoryEntity = new ContractAccessoryEntity();
            accessoryEntity.setAddView(isEdit);
            accessoryEntity.setPosition(4);
            accessoryEntity.setNetPic(true);
            accessoryEntity.setFileUrl(attachmentBean.getAttachment4());
            accessoryEntity.setId(attachmentBean.getAttachment4());
            accessoryEntity.setAttachment4(attachmentBean.getAttachment4());
            listData.add(accessoryEntity);
        }
        if (!TextUtils.isEmpty(attachmentBean.getAttachment5())) {
            ContractAccessoryEntity accessoryEntity = new ContractAccessoryEntity();
            accessoryEntity.setAddView(isEdit);
            accessoryEntity.setPosition(5);
            accessoryEntity.setNetPic(true);
            accessoryEntity.setFileUrl(attachmentBean.getAttachment5());
            accessoryEntity.setAttachment5(attachmentBean.getAttachment5());
            accessoryEntity.setId(attachmentBean.getAttachment5());
            listData.add(accessoryEntity);
        }
        if (!TextUtils.isEmpty(attachmentBean.getAttachment6())) {
            ContractAccessoryEntity accessoryEntity = new ContractAccessoryEntity();
            accessoryEntity.setAddView(isEdit);
            accessoryEntity.setNetPic(true);
            accessoryEntity.setPosition(6);
            accessoryEntity.setFileUrl(attachmentBean.getAttachment6());
            accessoryEntity.setAttachment6(attachmentBean.getAttachment6());
            accessoryEntity.setId(attachmentBean.getAttachment6());
            listData.add(accessoryEntity);
        }
        StoreUploadHepler.oldPicSize = listData.size();
        return listData;
    }




    public static ArrayList<Integer> getIndexList(SafeInspectEntity.ResultsBean.AttachmentsBean attachmentBean) {
        if (attachmentBean == null) {
            return null;
        }
        ArrayList<Integer> listData = new ArrayList<>();
        if (!TextUtils.isEmpty(attachmentBean.getAttachment1())) {
            listData.add(1);
        }
        if (!TextUtils.isEmpty(attachmentBean.getAttachment2())) {
            listData.add(2);
        }
        if (!TextUtils.isEmpty(attachmentBean.getAttachment3())) {
            listData.add(3);
        }
        if (!TextUtils.isEmpty(attachmentBean.getAttachment4())) {
            listData.add(4);
        }
        if (!TextUtils.isEmpty(attachmentBean.getAttachment5())) {
            listData.add(5);
        }
        if (!TextUtils.isEmpty(attachmentBean.getAttachment6())) {
            listData.add(6);
        }

        return listData;
    }*/
}
